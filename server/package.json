{
  "name": "drivesafe-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Express backend for DriveSafe: handles dashcam uploads, proxies to the PyTorch model, and wires the RAG explanation pipeline.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.0.0"
  }
}
